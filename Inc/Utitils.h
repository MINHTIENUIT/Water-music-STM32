#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "main.h"

uint8_t PWM_duty(uint8_t value, uint8_t max);

char* string_spectrum(uint8_t a[8]);

char* stringcat(char *dich, const char *nguon);
